#include <stdio.h>
#include <time.h>
#include "pathfinder.h"

// int compare(const void *a, const void *b) {
//     return *(int *)a - *(int *)b;
// }

int main() {
    // const size_t size = 1000000;
    // int *arr = malloc(sizeof(int) * size);

    // for (size_t i = 0; i < size; ++i)
    //     arr[i] = rand();

    // mx_sort_rec(arr, size, sizeof(int), compare);
    // printf("%i\n", mx_checksort(arr, size, sizeof(int), compare));
}
